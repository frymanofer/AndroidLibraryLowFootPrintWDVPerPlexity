// YourProject/android/build.gradle

buildscript {
    repositories {
        google()
        mavenCentral()
        // No need to include node_modules here
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:8.2.2'
    }
}

allprojects {
    repositories {
        mavenLocal()
        google()
        mavenCentral()
        // Incatude React Native's local Maven repository
//        maven {
//            url("$rootDir/../node_modules/react-native/android")
//        }
    }
}
